interface:
  display_name: "KimPossible"
  short_description: "Repo-specific patterns and workflows for KimPossible"
  default_prompt: "Use the KimPossible repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true