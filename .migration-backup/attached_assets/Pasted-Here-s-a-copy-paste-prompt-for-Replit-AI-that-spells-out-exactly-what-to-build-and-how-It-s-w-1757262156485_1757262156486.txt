Here’s a **copy-paste prompt** for Replit AI that spells out *exactly* what to build and how. It’s written as if you’re talking to Replit; drop it in and then paste/upload your images/PDFs when it asks.

---

# Replit AI — Build “Kiminou Knox” Luxury Site (No dead ends, real data)

**Non-negotiables (read carefully):**

* Replace **all** placeholder people images with **my photos** (I will upload).
* Use **real book data** (titles, dates, ISBNs, links, covers).
* Use **real PDFs** of my books (I will provide), with a clean preview + download flow.
* **No dead UI.** If it looks clickable (arrow, button, link, card, chip), it **must** do something meaningful.
* Luxury look & feel: editorial typography, refined spacing, high-contrast or luxe dark themes, glass/polish, cinematic motion.
* Accessibility (A11y) + performance are first-class.

---

## 0) Project context

* I already have a React + Tailwind + Framer Motion codebase with: MegaMenu, Hero (video), Doors (4 domains), Lookbook, BookPreview, Blog listing/posts (Drizzle schema present), and Theme Switcher.
* You will **upgrade** that project, not rebuild it from scratch.

---

## 1) Content ingestion (books & media) — use real assets

**Goal:** Replace all placeholder book content and images with real data and real covers; wire my book PDFs and retailer pages.

1. Create this content file and wire it into `BookPreview` and detail pages:

   ```
   /client/src/content/books.json
   ```

   **Shape:**

   ```json
   [
     {
       "id": "spirit-solomon",
       "title": "The Spirit of Solomon: What is Love To A Man Made To Destroy It",
       "subtitle": "Poetry",
       "isbn": "9798316662975",
       "publicationDate": "2025-04-03",
       "publisher": "Independently Published",
       "cover": "/img/books/spirit-of-solomon-cover.png",
       "pdf": "/books/spirit-of-solomon.pdf",
       "buyLinks": {
         "amazon": "<PASTE>",
         "googleBooks": "<PASTE>",
         "bookshop": "<PASTE>",
         "bn": "<PASTE>"
       },
       "tags": ["Love", "Hubris", "Growth"],
       "blurb": "<2–3 sentence official description>",
       "excerpt": "<short excerpt or back-cover quote>"
     },
     {
       "id": "our-father",
       "title": "Our Father?: A Poetic Journey Through Faith, Doubt, and Divine Silence",
       "subtitle": "Poetry",
       "isbn": "<ISBN if available>",
       "publicationDate": "2025-03-01",
       "publisher": "Independently Published",
       "cover": "/img/books/our-father-cover.png",
       "pdf": "/books/our-father.pdf",
       "buyLinks": {
         "amazon": "<PASTE>",
         "googleBooks": "<PASTE>",
         "bookshop": "<PASTE>",
         "bn": "<PASTE>"
       },
       "tags": ["Faith", "Doubt", "Resilience"],
       "blurb": "<official description>",
       "excerpt": "<short excerpt>"
     },
     {
       "id": "poems-black-boy",
       "title": "Poems from a Black Boy",
       "subtitle": "Poetry",
       "isbn": "<ISBN if available>",
       "publicationDate": "2025-01-01",
       "publisher": "Independently Published",
       "cover": "/img/books/poems-from-a-black-boy-cover.png",
       "pdf": "/books/poems-from-a-black-boy.pdf",
       "buyLinks": {
         "amazon": "<PASTE>",
         "googleBooks": "<PASTE>",
         "bookshop": "<PASTE>",
         "bn": "<PASTE>"
       },
       "tags": ["Identity", "Heritage", "Hope"],
       "blurb": "<official description>",
       "excerpt": "<short excerpt>"
     },
     {
       "id": "hopeless-romantic",
       "title": "Hopeless Romantic",
       "subtitle": "Poetry",
       "isbn": "9798291608128",
       "publicationDate": "2025-07-01",
       "publisher": "Independently Published",
       "cover": "/img/books/hopeless-romantic-cover.png",
       "pdf": "/books/hopeless-romantic.pdf",
       "buyLinks": {
         "amazon": "<PASTE>",
         "googleBooks": "<PASTE>",
         "bookshop": "<PASTE>",
         "bn": "<PASTE>"
       },
       "tags": ["Love", "Heartbreak", "Healing"],
       "blurb": "<official description>",
       "excerpt": "<short excerpt>"
     }
   ]
   ```

2. Create folders and expect me to upload **real assets**:

   ```
   /client/public/img/books/   (PNG/JPG covers I provide)
   /client/public/books/       (PDFs I provide)
   /client/public/img/me/      (portraits & action shots of me)
   ```

   * **Replace every placeholder “man” photo** with my images from `/img/me/`.
   * Do **not** hotlink book covers—use my uploaded images.

3. Update **BookPreview** and **Book detail** views to load from `books.json`.

   * For each book: show cover, title, tags, year (derive from `publicationDate`), CTA buttons: **Buy** (primary) and **Read Sample** (opens PDF modal or embedded preview with page thumbnails).
   * If `pdf` is present, the **Sample PDF** button must work.

---

## 2) Research & verification (links only; no scraping assumptions)

* Where you can, surface **real retailer links** (Amazon, Google Books/Play, Bookshop, B\&N) and ask me to confirm.
* If a link is uncertain, set it to `TODO:` and list it in a “Content TODO” panel during dev.
* Prefer **owner-provided** PDFs and covers (I’m the rights holder). Do not fetch random PDFs.

---

## 3) Visual upgrades (luxury, editorial, interactive)

Implement these upgrades across the app (keep it tasteful):

**Typography & Theme**

* Add two font stacks:

  * Display serif (e.g., `Playfair Display` or `Cormorant Garamond`) for H1/H2.
  * Workhorse sans (e.g., `Inter` or `Suisse Intl`) for body/UI.
* Keep/extend theme tokens (`--bg, --fg, --muted, --border, --panel, --accent`) per theme:

  * **Maison (light, editorial)**
  * **Noir (dark, luxe)**
  * **Editorial (soft gray)**
  * **Street (bold accent)**
* Increase line-height and negative space; ensure hairline borders are consistent.

**Motion & Micro-interactions**

* Framer Motion page transitions (fade+slight slide).
* **MegaMenu**: ultra-smooth open/close, focus-trap, Esc to close, arrow-key nav.
* **Magnetic buttons** (gentle attraction on hover) for primary CTAs.
* **Card tilt** (3D, 2–3°) on “Doors” and book cards; disable on touch devices.
* **Hover underline** that animates from center outward for nav links.
* **Custom cursor dot** (small circle) that enlarges on interactive elements; respect `prefers-reduced-motion`.
* **Mesh/radial background** layer behind BookPreview and Hero (very subtle).

**Hero & media**

* Keep video hero with poster fallback; add blur-into-focus on load.
* Add “Skip animation” link when `prefers-reduced-motion` is set.

---

## 4) Functionality upgrades (no dead ends)

* **Global No-Dead-UI policy:**
  Implement a **link/interaction audit**:

  * Add a script `/client/scripts/audit-interactions.ts` using Playwright to scan the DOM for any element with role `button|link` or class `btn|arrow|cta|chip|card` and **assert** it has either `href` or a bound click handler that routes/opens something. Fail the test if not.
* **Routes that must exist:**

  * `/` (home)
  * `/books` (grid of books from `books.json`)
  * `/books/:id` (book detail with cover, metadata, description, excerpt, buy links, embedded PDF preview)
  * `/blog` (list; category & tag filtering)
  * `/blog/:slug` (post detail)
  * `/contact` (form with email handoff; see below)
* **PDF previewer:**
  Use a lightweight PDF renderer (e.g., `pdfjs-dist`) inside a modal on book detail pages with thumbnails + “Open full PDF” link (downloads the file in `/public/books/…`).
  Respect file size; lazy-load pages.
* **Contact form:**
  Implement `/api/contact` endpoint (POST) that:

  * Validates: name, email, message.
  * Sends email via provider (stub function for now; log on dev).
    UX: inline success/fail, never dead.

---

## 5) Blog & CMS polish (Drizzle already present)

* Continue using Drizzle schemas you have; **hash passwords** before storing (bcrypt or argon2). Never store plaintext.
* In `BlogAdmin`, add:

  * Image upload for `featuredImage`.
  * Tag chips with autocomplete.
  * **Preview mode** (render Markdown).
* Blog list:

  * Add **search**, **tag filter**, **category filter**; maintain query in URL.
  * Skeleton loaders (shimmer) while fetching.
  * Card click or “Read” button always routes to a working post page.

---

## 6) Accessibility & performance (hard requirements)

* Respect `prefers-reduced-motion`.
* Keyboard support:

  * Tab order, visible focus rings.
  * MegaMenu focus trap + ESC close.
* Color contrast AA at minimum.
* Image `alt` text and `aria-label` on icon-only buttons.
* Lighthouse targets (mobile):

  * Performance ≥ 90
  * A11y ≥ 95
  * Best Practices ≥ 95
  * SEO ≥ 90
* Perf guardrails:

  * LCP < 2.5s
  * CLS < 0.1
  * JS bundle under control; lazy-load heavy components (PDF viewer, lookbook).

---

## 7) File changes & additions

* `/client/src/content/books.json` (source of truth for books)
* `/client/public/img/books/*` (real covers I provide)
* `/client/public/books/*` (real PDFs I provide)
* **Components** (extend/refactor as needed):

  * `components/BookPreview.tsx` (read from books.json, new design)
  * `pages/Books.tsx`, `pages/BookDetail.tsx` (new routes)
  * `components/PDFModal.tsx` (pdfjs viewer)
  * `components/Cursor.tsx` (custom cursor)
  * `components/MagneticButton.tsx`
  * `components/UnderlineLink.tsx`
* **Scripts/Tests**

  * `scripts/audit-interactions.ts` (Playwright link/click audit)
  * `scripts/lighthouse-ci.mjs` (optional CI gate)

---

## 8) QA checklist (must pass before done)

1. **Books**

   * All covers render from `/img/books/*`.
   * All **Buy** buttons open real retailer pages (Amazon/Google/Bookshop/B\&N).
   * **Sample PDF** opens readable preview; download works. (Use my uploaded PDFs.)
2. **Images of me**

   * Every human photo on site is my image from `/img/me/*`.
3. **No dead ends**

   * Run `npm run audit:interactions` — zero failures.
4. **Navigation**

   * All menu items route; MegaMenu works on hover, click, keyboard; ESC closes.
5. **Accessibility**

   * Tab through entire homepage; focus visible; color contrast AA; images have alt.
6. **Performance**

   * Mobile Lighthouse meets targets; hero video does not block LCP.
7. **Reduced motion**

   * With `prefers-reduced-motion`, animations and video degrade gracefully.

---

## 9) Implementation notes

* Ask me immediately to **upload**:

  * 4 **book covers** (`/img/books/*.png`)
  * 4 **book PDFs** (`/books/*.pdf`)
  * **Portraits/action photos** (`/img/me/*.jpg`)
  * **Retailer links** (Amazon/Google/Bookshop/B\&N) for each book
* Do **not** invent dates/titles/links. If missing, set to `TODO:` and list them in a dev “Content TODO” panel.
* Keep aesthetics **luxury editorial**: generous spacing, hairline borders, smooth but understated motion, glass surfaces, custom cursor, gentle card tilt, and marquee subtlety.

---

## 10) Commands

* `npm i`
* `npm run dev`
* `npm run audit:interactions` (Playwright)
* `npm run build`

**Deliverable:** a running project with my real book data wired, all assets replaced, no dead UI, luxury aesthetics throughout, and passing QA checklist.

---

When you’re ready, tell me exactly which files or links you still need from me (covers, PDFs, retailer URLs), and I’ll upload them immediately.
