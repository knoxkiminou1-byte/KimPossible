Prompt: "I need you to optimize my existing personal brand website for Kiminou Knox to restore my Google Knowledge Panel. Apply these updates precisely:

Google Verification: Add this meta tag to the <head> of my index.html: <meta name="google-site-verification" content="-w34ED6Qb8_GmZzRIjJVBfhhNZkE4OAh5Z-V97KsYXU------">.

Master Identity Schema: Add a JSON-LD script to the <head> identifying me as a 'Person' entity. Use these details:

Name: Kiminou Knox

Heritage: African American, Jamaican, and Congolese.

Careers: Author (6-time published, 'Spirit of Solomon', 'Black Boy Lie' universe), Athlete (6'7-6'8 basketball player), and Entrepreneur.

sameAs Links: MaxPreps (Ygnacio Valley profile), NCSA, Prep Hoops, Goodreads, and my Instagram.

Backend Hardening: In index.js, ensure the server uses process.env.PORT || 3000 and '0.0.0.0' to prevent deployment crashes. Install and use helmet for security and compression for speed.

SEO Files: Generate a robots.txt that allows all crawlers and a dynamic /sitemap.xml route that Google can read.

Open Graph: Add meta tags for social sharing that highlight my triple-threat identity as Author/Athlete/Entrepreneur."

2. The "Hard-Coded" Verification Tag
If you prefer to just paste the code yourself into your index.html file, here is exactly how that tag should look. Place it right at the top of your <head>:

HTML

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
  <meta name="google-site-verification" content="-w34ED6Qb8_GmZzRIjJVBfhhNZkE4OAh5Z-V97KsYXU------">
  
  <title>Kiminou Knox | Author • Athlete • Entrepreneur</title>
</head>
3. Final Backend Logic for your index.js
This is the "Engine Room." It ensures your site is fast and never fails to deploy on Replit.

JavaScript

const express = require('express');
const helmet = require('helmet');
const compression = require('compression');
const path = require('path');
const app = express();

// 1. PERFORMANCE & SECURITY (Crucial for Google Trust)
app.use(helmet()); 
app.use(compression()); 
app.use(express.static('public'));

// 2. SITEMAP (Tells Google where everything is)
app.get('/sitemap.xml', (req, res) => {
  res.header('Content-Type', 'application/xml');
  res.send(`<?xml version="1.0" encoding="UTF-8"?>
    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
      <url><loc>https://kiminouknox.com/</loc><priority>1.0</priority></url>
    </urlset>`);
});

// 3. THE REPLIT DEPLOYMENT PROTECTOR
const PORT = process.env.PORT || 3000;
app.listen(PORT, '0.0.0.0', () => {
  console.log(`Kiminou Knox Entity Home is live on port ${PORT}`);
});